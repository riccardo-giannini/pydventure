print('look controller')
lookAt = input('What do you want to look at?')
if lookAt == '':
    # look around
    # TODO: try to explain the use of pass (especially from the 'writing code'
    # point of view)
    pass
else:
    # What room are we in?
    # Does an object in the room have the name stored in lookAt?
    #   yes: describe it
    #   no: return error
    pass